package com.ppfuns.model.adapter;

import android.support.v7.widget.RecyclerView.Adapter;
import android.view.KeyEvent;
import android.view.View;

/**
 * Created by hmc on 2016/7/1.
 */
public interface OnItemActionListener {

    void onItemClick(Adapter adapter, View view, int position);

    void onFocusChange(Adapter adapter, View view, boolean focused, int position);

    void onKey(Adapter adapter, View view, int keyCode, KeyEvent event, int position);
}
