package com.ppfuns.vod.receiver;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.text.TextUtils;

import com.ppfuns.util.LogUtils;
import com.ppfuns.vod.activity.PlayerActivity;

/**
 * Created by hmc on 2016/9/2.
 */
public class PlayControlReceiver extends BroadcastReceiver{

    private final String TAG = PlayControlReceiver.class.getSimpleName();

    private static final String ACTION = "com.ppfuns.vod.playcontrol";

    @Override
    public void onReceive(Context context, Intent intent) {
        LogUtils.i(TAG, "onReceive");
        if (intent != null) {
//            if (TextUtils.equals(intent.getAction(), ACTION)) {
//                Intent iIntent = new Intent(context,PlayerActivity.class);
//                iIntent.putExtra("order", "next");
//                iIntent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
//                context.startActivity(iIntent);
//            }
        }
    }
}
